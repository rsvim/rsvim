//! Container is a special widget that only has a shape, but no actual content. It's a logical
//! container that manages all its nested children widgets, and arranges their layout (todo).

pub mod root;
pub mod window;
